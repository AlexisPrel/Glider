/*
* This program calls the main function do to the computations 
* and simply prints l(v0) in the output file.
*/

#include <stdio.h>
double glider (double v, double th, double x, double y);

int
main (void)
{
    double v0;
    for (v0 = 0.5; v0 <= 5; v0 += 0.25)
        printf ("%f %f\n", v0, glider (v0, 0., 0., 2.));
    return 0;
}
